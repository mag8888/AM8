# 🗄️ Настройка Railway MongoDB для карточных колод

## ✅ Что было сделано

Код обновлен для поддержки следующих переменных окружения (в порядке приоритета):
1. `RAILWAY_MONGODB_URI` - основной URI Railway MongoDB
2. `MONGO_URL` - альтернативный URI Railway MongoDB
3. `MONGO_PUBLIC_URL` - публичный URI Railway MongoDB
4. `MONGODB_URI` - резервный URI (MongoDB Atlas)

## 📋 Инструкция по настройке в Railway

### Шаг 1: Откройте настройки MongoDB сервиса в Railway

1. Перейдите в ваш проект на Railway
2. Найдите сервис **MongoDB**
3. Откройте вкладку **Variables**

### Шаг 2: Установите переменную окружения

Добавьте одну из следующих переменных:

**Вариант 1: Использовать MONGO_URL (рекомендуется)**
```
MONGO_URL=mongodb://mongo:XFAMLKzevqVGJxxIXcXLOCSonYXKtWxT@interchange.proxy.rlwy.net:55875
```

**Вариант 2: Использовать RAILWAY_MONGODB_URI**
```
RAILWAY_MONGODB_URI=mongodb://mongo:XFAMLKzevqVGJxxIXcXLOCSonYXKtWxT@interchange.proxy.rlwy.net:55875
```

**Вариант 3: Использовать MONGO_PUBLIC_URL (если доступен)**
```
MONGO_PUBLIC_URL=mongodb://mongo:XFAMLKzevqVGJxxIXcXLOCSonYXKtWxT@interchange.proxy.rlwy.net:55875
```

### Шаг 3: Установите переменную в сервисе приложения

1. Перейдите в сервис вашего приложения (AM8_serv или аналогичный)
2. Откройте вкладку **Variables**
3. Добавьте ту же переменную окружения, что и выше

### Шаг 4: Перезапустите сервис

После установки переменной окружения:
1. Railway автоматически перезапустит сервис
2. Или нажмите **Redeploy** в настройках сервиса

## 🔍 Проверка подключения

После перезапуска проверьте логи сервиса. Вы должны увидеть:

```
📊 Database: MONGO_URL: SET
📊 Database: Используется MONGO_URL
📊 Database: Подключение к Railway MongoDB...
✅ Database: Успешно подключено к Railway MongoDB
✅ MongoDB подключена
✅ Карточные колоды инициализированы
```

## ⚠️ Если что-то не работает

1. **Проверьте формат URI**: Убедитесь, что строка подключения начинается с `mongodb://`
2. **Проверьте переменные**: Убедитесь, что переменная установлена в правильном сервисе
3. **Проверьте логи**: Ищите ошибки подключения в логах Railway
4. **Проверьте права доступа**: Убедитесь, что пользователь имеет права на чтение/запись в базу данных

## 📝 Примечания

- Код автоматически определяет тип подключения (Railway MongoDB или MongoDB Atlas)
- Приоритет переменных: `RAILWAY_MONGODB_URI` > `MONGO_URL` > `MONGO_PUBLIC_URL` > `MONGODB_URI`
- Если ни одна переменная не установлена, код попытается использовать fallback конфигурацию (MongoDB Atlas)

